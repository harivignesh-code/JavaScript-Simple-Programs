import React from 'react'
import { Button } from 'react-bootstrap'

const Mob = ({abs}) => {
  return (
    <div>
        <Button>
        <p>{abs}</p>
        </Button>   
    </div>
  )
}

export default Mob